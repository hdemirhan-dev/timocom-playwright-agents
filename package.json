{
  "name": "timocom-playwright-agents",
  "version": "1.0.0",
  "description": "Playwright test suite for timocom.de — generated by Playwright Agents (--loop=claude)",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.56.0",
    "dotenv": "^17.4.2"
  }
}
